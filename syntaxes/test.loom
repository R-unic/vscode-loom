trait Greet {
  fn greet(): string
}

interface Person {
  name: string
}

implement Greet for Person {
  fn greet() -> "Hello, #{name}!";
}

fn say_hello(greetable: Greet) -> print(greetable.greet());

let person: Person = { name: "Runic" };
say_hello(person); ## Hello, Runic!